<div class="space-y-4"><h1 class="text-2xl font-semibold">Projects</h1><div class="grid md:grid-cols-2 xl:grid-cols-3 gap-4">@forelse($projects as $project)<x-card><p class="font-semibold">{{ $project->name }}</p><p class="text-sm text-slate-500">Budget: ${{ number_format($project->budget,2) }}</p><x-status-pill :status="$project->status" /></x-card>@empty<x-empty-state title="No projects" />@endforelse</div>{{ $projects->links() }}</div>
